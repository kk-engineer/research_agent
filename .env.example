# =============================================================================
# Research Agent — Secrets (API keys only)
# =============================================================================
# Copy this file to .env and fill in your API keys.
# Non-secret configuration lives in config.toml at the project root.

# --- LLM API Key (required for LLM_PROVIDER=openai) -------------------------
# LLM_API_KEY=sk-your-openai-api-key

# --- Web Search -------------------------------------------------------------
# Required when SEARCH_PROVIDER=tavily (default)
TAVILY_API_KEY=tvly-dev-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
